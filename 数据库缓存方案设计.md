# Tushare MCP Server - 数据库缓存方案设计

## 📋 需求变更

**原方案：** 使用JSON文件存储缓存  
**新需求：** 使用关系型数据库或KV数据库存储缓存

## 🔍 方案对比分析

### 方案一：SQLite（关系型数据库）⭐ 推荐

**优势：**
- ✅ **零配置**：Python标准库内置支持，无需安装额外依赖
- ✅ **轻量级**：单文件数据库，无需服务器进程
- ✅ **性能优秀**：适合中小规模数据，查询速度快
- ✅ **SQL支持**：可以使用SQL进行复杂查询和统计
- ✅ **事务支持**：保证数据一致性
- ✅ **易于管理**：可以使用SQL工具查看和管理数据
- ✅ **跨平台**：Windows/Linux/macOS都支持

**适用场景：**
- 缓存数据量：中小规模（几千到几万条记录）
- 查询模式：按条件查询、统计、清理
- 部署要求：单机部署，无需外部服务

### 方案二：Redis（KV数据库）

**优势：**
- ✅ **高性能**：内存数据库，读写速度极快
- ✅ **丰富的数据结构**：支持String、Hash、List、Set等
- ✅ **过期策略**：内置TTL机制，自动过期
- ✅ **持久化**：支持RDB和AOF持久化

**劣势：**
- ❌ **需要外部服务**：需要安装和运行Redis服务器
- ❌ **增加依赖**：需要安装redis-py库
- ❌ **部署复杂**：需要配置Redis服务
- ❌ **资源占用**：常驻内存进程

**适用场景：**
- 大规模缓存（百万级记录）
- 需要分布式缓存
- 已有Redis基础设施

### 方案三：其他KV数据库（LevelDB、RocksDB等）

**劣势：**
- ❌ **需要额外依赖**：需要安装C++库和Python绑定
- ❌ **配置复杂**：比SQLite复杂
- ❌ **文档较少**：社区支持不如SQLite

## 🎯 最终方案：SQLite ✅

**已确认使用SQLite方案**

基于项目特点，**使用SQLite**，原因：
1. ✅ **完全免费**：公共领域许可证，商业使用无需付费
2. ✅ **零依赖**：Python标准库内置支持，无需安装额外依赖
3. ✅ **轻量级**：单文件数据库，无需服务器进程
4. ✅ **性能优秀**：适合中小规模数据，查询速度快
5. ✅ **SQL支持**：可以使用SQL进行复杂查询和统计
6. ✅ **事务支持**：保证数据一致性
7. ✅ **易于管理**：可以使用SQL工具查看和管理数据

## 🏗️ SQLite缓存方案设计

### 1. 数据库结构

**表名：`cache_data`**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `cache_key` | TEXT PRIMARY KEY | 缓存键（MD5哈希） |
| `cache_type` | TEXT | 缓存类型（stock_basic/stock_search/income_statement） |
| `params_json` | TEXT | 查询参数的JSON字符串 |
| `data_json` | TEXT | 缓存数据的JSON字符串（DataFrame转为JSON） |
| `created_at` | REAL | 创建时间戳 |
| `expires_at` | REAL | 过期时间戳 |
| `access_count` | INTEGER | 访问次数（用于统计） |
| `last_accessed` | REAL | 最后访问时间 |

**索引：**
- `idx_cache_type`: `cache_type`
- `idx_expires_at`: `expires_at`（用于快速清理过期数据）

### 2. 核心功能设计

#### 2.1 CacheManager 类（基于SQLite）

```python
class CacheManager:
    def __init__(self, db_path: Path):
        """初始化缓存管理器，连接SQLite数据库"""
        self.db_path = db_path
        self.conn = sqlite3.connect(db_path, check_same_thread=False)
        self._init_database()
    
    def _init_database(self):
        """创建表和索引"""
        # 创建表
        # 创建索引
    
    def get_dataframe(self, cache_type: str, **params) -> Optional[pd.DataFrame]:
        """获取缓存的DataFrame"""
        # 1. 生成缓存键
        # 2. 查询数据库
        # 3. 检查是否过期
        # 4. 更新访问统计
        # 5. 返回DataFrame或None
    
    def set(self, cache_type: str, data: pd.DataFrame, **params) -> bool:
        """保存DataFrame到缓存"""
        # 1. 生成缓存键
        # 2. DataFrame转为JSON
        # 3. 计算过期时间
        # 4. INSERT OR REPLACE到数据库
    
    def clear(self, cache_type: Optional[str] = None) -> int:
        """清理缓存"""
        # DELETE FROM cache_data WHERE cache_type = ?
    
    def cleanup_expired(self) -> int:
        """清理过期缓存"""
        # DELETE FROM cache_data WHERE expires_at < ?
    
    def get_stats(self) -> dict:
        """获取缓存统计信息"""
        # SELECT cache_type, COUNT(*), SUM(access_count) FROM cache_data GROUP BY cache_type
```

#### 2.2 数据库操作优化

1. **连接管理**：使用连接池或单例模式，避免频繁创建连接
2. **批量操作**：清理过期缓存时使用批量DELETE
3. **WAL模式**：启用Write-Ahead Logging提升并发性能
4. **参数化查询**：防止SQL注入，提升性能

### 3. 集成方式（与JSON方案相同）

```python
def get_stock_basic_info(ts_code="", name=""):
    # 1. 尝试从数据库缓存获取
    df = cache_manager.get_dataframe('stock_basic', ts_code=ts_code, name=name)
    
    if df is None:
        # 2. 缓存未命中，调用API
        df = pro.stock_basic(**filters)
        # 3. 保存到数据库
        cache_manager.set('stock_basic', df, ts_code=ts_code, name=name)
    
    return format_result(df)
```

### 4. 数据库文件位置

```
项目根目录/
  └── .cache/
      └── cache.db          # SQLite数据库文件
```

### 5. 缓存过期策略（保持不变）

| 缓存类型 | 过期时间 | SQL查询示例 |
|---------|---------|------------|
| `stock_basic` | 24小时 | `expires_at = created_at + 86400` |
| `stock_search` | 12小时 | `expires_at = created_at + 43200` |
| `income_statement` | 7天 | `expires_at = created_at + 604800` |

### 6. 新增功能（数据库优势）

#### 6.1 缓存统计工具
```python
@mcp.tool()
def get_cache_stats() -> str:
    """获取缓存统计信息"""
    stats = cache_manager.get_stats()
    # 返回：各类型缓存数量、总访问次数、缓存命中率等
```

#### 6.2 按条件清理
```python
@mcp.tool()
def clear_cache_by_date(before_date: str = "") -> str:
    """清理指定日期之前的缓存"""
    # DELETE FROM cache_data WHERE created_at < ?
```

#### 6.3 查看缓存详情
```python
@mcp.tool()
def list_cache(cache_type: str = "", limit: int = 10) -> str:
    """列出缓存条目"""
    # SELECT * FROM cache_data WHERE cache_type = ? ORDER BY last_accessed DESC LIMIT ?
```

## 📦 依赖变更

### SQLite方案
**无需新增依赖**，Python标准库已包含 `sqlite3` 模块

### Redis方案（备选）
如果选择Redis，需要添加：
```
requirements.txt:
redis>=5.0.0
```

## 🔄 迁移方案

如果未来需要从SQLite迁移到Redis：
1. 保持 `CacheManager` 接口不变
2. 只修改内部实现，从SQLite改为Redis
3. 提供数据迁移脚本（可选）

## 📊 性能对比（预估）

| 操作 | JSON文件 | SQLite | Redis |
|------|----------|--------|-------|
| 读取单条 | ~1-5ms | ~0.5-2ms | ~0.1-0.5ms |
| 写入单条 | ~2-10ms | ~1-3ms | ~0.1-0.5ms |
| 批量清理 | ~100-500ms | ~10-50ms | ~5-20ms |
| 统计查询 | 需遍历文件 | SQL查询快 | 需额外实现 |

## ✅ SQLite方案优势总结

1. **零依赖**：Python标准库支持
2. **高性能**：比JSON文件快，满足缓存需求
3. **易管理**：可以用SQL工具查看和管理
4. **功能丰富**：支持统计、复杂查询
5. **数据安全**：事务支持，数据一致性
6. **易于调试**：可以用SQLite浏览器工具查看数据

## 🎯 推荐方案对比

| 特性 | SQLite ⭐ | Redis | JSON文件 |
|------|----------|-------|----------|
| 零依赖 | ✅ | ❌ | ✅ |
| 性能 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 易用性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 功能丰富 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 部署复杂度 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |

## ❓ 待确认问题

1. **数据库选择**：是否同意使用SQLite？
   - 如果同意，使用SQLite（推荐）
   - 如果需要更高性能，可以考虑Redis（但需要额外配置）

2. **数据库文件位置**：是否同意使用 `.cache/cache.db`？

3. **统计功能**：是否需要添加缓存统计功能（命中率、缓存数量等）？

4. **数据迁移**：如果未来需要切换数据库，是否需要保留迁移方案？

---

**推荐使用SQLite方案，零依赖、高性能、易管理。请确认是否采用此方案。**

